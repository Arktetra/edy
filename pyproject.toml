[project]
name = "edy"
version = "0.1.0"
dependencies = [
    "opencv-python",
    "numpy",
    "matplotlib",
]

[project.urls]
Repository = "https://github.com/Arktetra/edy.git"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
